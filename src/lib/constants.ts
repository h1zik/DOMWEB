/** Re-export untuk kompatibilitas impor lama. Prefer `getSiteConfig()` / `defaultSiteConfig`. */
export type { Brand, SiteConfig } from "./site-config-schema";
export { defaultSiteConfig } from "./default-site-config";
